import type { ReactNode } from "react";

export const metadata = {
  title: "Triptrackr • Search",
};

export default function SearchLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
