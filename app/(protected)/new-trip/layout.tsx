import type { ReactNode } from "react";

export const metadata = {
  title: "Triptrackr • New Trip",
};

export default function NewTripLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
